using Kontakt.DAL;
using Kontakt.Services;
using Moq;
using System.Net.Http;
using Xunit;

namespace Kontakt.JUnits
{
    public class TestOne
    {
        [Fact]
        public void foo()
        {
            //var fakeMock = new AppDbContext();
            //var layouteService = new LayoutService(null,fakeMock);

            //    List<Category> categories = await _context.Categories
            //    .Include(x => x.CategoryBrands).ThenInclude(x => x.Brand)
            //    .Include(x => x.Children)
            //    .ToListAsync();

        }
    }
}
